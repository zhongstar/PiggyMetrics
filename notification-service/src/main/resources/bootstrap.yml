spring:
  application:
    name: notification-service
  cloud:
    config:
      uri: http://${CONFIG_HOST}:${CONFIG_PORT}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user