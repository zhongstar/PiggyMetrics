spring:
  application:
    name: auth-service
  cloud:
    config:
      uri: http://${CONFIG_HOST}:${CONFIG_PORT}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
