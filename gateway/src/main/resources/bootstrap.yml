spring:
  application:
    name: gateway
  cloud:
    config:
      uri: http://${CONFIG_HOST}:${CONFIG_PORT}
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user
management: 
  security: 
    enabled: false
